# Optional: pre-fill these to act as the "house" key.
# If unset, users must enter their own keys via the UI (saved to localStorage).
# These server keys are used only when a user has NOT set their own keys.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
